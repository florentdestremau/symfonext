<form method="post" action="{{ path('app_briefcase_delete', {'id': briefcase.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ briefcase.id) }}">
    <button class="btn">Delete</button>
</form>
